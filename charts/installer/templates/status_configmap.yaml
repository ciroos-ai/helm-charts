apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "installer.fullname" . }}-bootstrap-status
  labels:
  {{- include "installer.labels" . | nindent 4 }}
data:
  last-bootstrap: {{ now | date "2006-01-02T15:04:05Z07:00" | quote }}
  chart-version: {{ .Chart.Version | quote }}