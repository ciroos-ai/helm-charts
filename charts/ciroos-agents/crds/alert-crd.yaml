apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: alerts.lib.ciroos.ai
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
spec:
  group: lib.ciroos.ai
  names:
    kind: Alert
    listKind: AlertList
    plural: alerts
    singular: alert
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Alert is the Schema for the alerts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Alert
            properties:
              matchRules:
                description: MatchRules defines CEL expressions to filter event
                items:
                  description: Rule represents a CEL expression
                  properties:
                    expression:
                      description: Expression is a CEL expression
                      type: string
                    name:
                      description: Name is a unique identifier for the rule
                      type: string
                  required:
                  - expression
                  - name
                  type: object
                type: array
            type: object
          status:
            description: status defines the observed state of Alert
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status: {}