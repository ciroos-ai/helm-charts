apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
    annotations:
        controller-gen.kubebuilder.io/version: v0.20.1
    name: namespacedalerts.lib.ciroos.ai
spec:
    group: lib.ciroos.ai
    names:
        kind: NamespacedAlert
        listKind: NamespacedAlertList
        plural: namespacedalerts
        singular: namespacedalert
    scope: Namespaced
    versions:
        - name: v1beta1
          schema:
            openAPIV3Schema:
                description: NamespacedAlert is the Schema for the namespacedalerts API
                properties:
                    apiVersion:
                        description: |-
                            APIVersion defines the versioned schema of this representation of an object.
                            Servers should convert recognized schemas to the latest internal value, and
                            may reject unrecognized values.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                        type: string
                    kind:
                        description: |-
                            Kind is a string value representing the REST resource this object represents.
                            Servers may infer this from the endpoint the client submits requests to.
                            Cannot be updated.
                            In CamelCase.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                    metadata:
                        type: object
                    spec:
                        description: spec defines the desired state of NamespacedAlert
                        properties:
                            matchRules:
                                description: MatchRules defines CEL expressions to filter event
                                items:
                                    description: Rule represents a CEL expression
                                    properties:
                                        expression:
                                            description: Expression is a CEL expression
                                            type: string
                                        name:
                                            description: Name is a unique identifier for the rule
                                            type: string
                                    required:
                                        - expression
                                        - name
                                    type: object
                                type: array
                        type: object
                    status:
                        description: status defines the observed state of NamespacedAlert
                        type: object
                required:
                    - spec
                type: object
          served: true
          storage: true
          subresources:
            status: {}