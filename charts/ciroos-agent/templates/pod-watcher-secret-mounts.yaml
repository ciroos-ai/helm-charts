{{- if .Values.namespaceScoped }}
apiVersion: lib.ciroos.ai/v1beta1
kind: NamespacedResourceWatcher
metadata:
  name: pod-watcher-secret-mounts
  labels:
  {{- include "ciroos-agents.labels" . | nindent 4 }}
spec:
  alwaysSend: false
  resources:
  - dependencyRules:
    - direction: outbound
      expression: |
        has(resource.spec.volumes) ?
          resource.spec.volumes
            .filter(vol, has(vol.secret))
            .map(vol, {
              "kind": "Secret",
              "apiVersion": "v1",
              "name": vol.secret.secretName,
              "namespace": resource.metadata.namespace
            }) : []
      name: secret-mounts
      type: mount
    groupVersionKind:
      group: ""
      kind: Pod
      version: v1
    matchRules:
    - expression: "true"
      name: all-pods
    name: running-pods
{{- else }}
apiVersion: lib.ciroos.ai/v1beta1
kind: ResourceWatcher
metadata:
  name: pod-watcher-secret-mounts
  labels:
  {{- include "ciroos-agents.labels" . | nindent 4 }}
spec:
  alwaysSend: false
  resources:
  - dependencyRules:
    - direction: outbound
      expression: |
        has(resource.spec.volumes) ?
          resource.spec.volumes
            .filter(vol, has(vol.secret))
            .map(vol, {
              "kind": "Secret",
              "apiVersion": "v1",
              "name": vol.secret.secretName,
              "namespace": resource.metadata.namespace
            }) : []
      name: secret-mounts
      type: mount
    groupVersionKind:
      group: ""
      kind: Pod
      version: v1
    matchRules:
    - expression: "true"
      name: all-pods
    name: running-pods
{{- end }}