apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-k8smcp-egress-to-k8s-api
  labels:
  {{- include "ciroos-agents.labels" . | nindent 4 }}
spec:
  egress:
  - ports:
    - port: 443
      protocol: TCP
    - port: 6443
      protocol: TCP
  podSelector:
    matchLabels:
      app: k8smcp
  policyTypes:
  - Egress